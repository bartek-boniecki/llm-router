apiVersion: v1
kind: Service
metadata:
  name: router-svc
  namespace: llm-router
spec:
  selector:
    app: router
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  type: ClusterIP
